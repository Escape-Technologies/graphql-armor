import { defineUserConfig } from 'vuepress';

export default defineUserConfig({
  lang: 'en-US',
  title: 'GraphQL Armor Documentation',
  description:
    'Documentation of GraphQL Armor, The missing GraphQL security security layer for GraphQL Node ecosystem.',
});
