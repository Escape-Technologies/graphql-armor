{
  "steps": [
    {
      "name": "build",
      "command": "yarn workspace @escape.tech/graphql-armor build"
    },
    {
      "name": "test",
      "command": "cd $(git rev-parse --show-toplevel) && yarn test --testPathPattern=packages/graphql-armor"
    }
  ]
}
